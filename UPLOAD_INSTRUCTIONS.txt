THE COMMANDER FORGE — SPLIT/MULTI-FACE CARD FIX

This fixes errors such as:
Missing card data: Commit // Memory

Upload all five code files in this folder to the TOP LEVEL of your GitHub repository:
- api.js
- main.js
- constants.js
- sw.js
- build_precons.py

They should replace the files with the same names.

Keep:
.github/workflows/main.yml

After committing:
1. Open GitHub Actions.
2. Wait for Deploy The Commander Forge to finish with a green check.
3. Confirm the run includes “Build same-origin official precon catalog.”
4. Reopen the website and hard-refresh it.

What changed:
- Precon files now preserve Scryfall card IDs and Oracle IDs from MTGJSON.
- Split, transform, meld, adventure, and other multi-face cards are matched by ID.
- If an ID is unavailable, the app retries exact and fuzzy Scryfall name lookups.
- Precon search shows up to 100 matching results instead of 30.
- Old precon/card caches are versioned out so stale missing-card results are not reused.
